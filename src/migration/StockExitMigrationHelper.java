package migration;

public class StockExitMigrationHelper {
	
	/*
	select id, fecha, sucursal_id, prod_id, cantidad, detalle, procesa_id, autoriza_id, estado
from salidas
where procesa_id is not null;
	 */

}
